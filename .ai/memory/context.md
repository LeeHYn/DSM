# 현재 프로젝트 맥락

- **상태**: 마일스톤 12A 완료. 12B backend·지속형 local DB·change-gate 완료; 실제 FCM dispatch는 12C 전 비활성. Auth + Task/Category CRUD + refresh O(1) + DailyScore + ranking + FCM token/schedule + per-device dispatcher 구현.
- **검증 기준선(2026-07-20)**: backend Jest 22 suites·198 tests, e2e 1 suite·2 tests, direct AppModule compile, backend TypeScript, scoped lint·Prettier, Prisma validate·2 migrations up-to-date·zero drift 통과.
- **대상/stack**: `DSM_Back` NestJS + Prisma v6 + PostgreSQL UTC `timestamptz`; `DSM_Front` React Native + Expo Router SDK 55. Jest는 `tsconfig.spec.json` CommonJS. test에서 Prisma 실제 연결 차단.
- **Auth**: `@nestjs/jwt`, bcrypt refresh hash, Google/Kakao. `GOOGLE_CLIENT_ID` 필수·audience 검증. refresh `<recordId>.<secret>`, conditional revoke 단일 승자 + replacement create 동일 transaction. Apple 검증은 계정 확보 후.
- **Task/Category/Score**: Task mutation·schedule sync·score recompute 동일 Serializable transaction, Prisma `P2034` 최대 2회 재시도. actor 소유/default Category만 할당. UTC day score 10/20/30 × 1.5/1.3/1.0/0.7, cap 900, 6 tiers.
- **Ranking**: DAILY/WEEKLY/TOTAL 실시간 계산, leaderboard·snapshot API. Redis/batch/WebSocket 미구현.
- **알림 12A**: FCM token 등록·same-user 갱신·revoke. foreign-owner token/FID in-place 이전은 mutation 전 409. Task 상태와 `NotificationSchedule` 원자 동기화.
- **알림 12B**: ADC-only provider, 30초 Cron, schedule 100·delivery 500, 5분 lease·60초 heartbeat, per-device 결과·최대 3회 명시적 failure retry. `sendStartedAt` 이후 모호 결과는 terminal `UNKNOWN`; 자동 재발송 금지. payload는 account-neutral data-only `REMINDER_SYNC`/version.
- **finding**: F-004·F-010·F-014 포함 12건 `RECHECKED`; F-007 send recall 불가·client 표시 직후 취소 race만 사용자 `ACCEPTED_RISK`. 12C 전 `FCM_DISPATCH_ENABLED=false`.
- **local DB**: Docker Desktop 4.82.0, Engine/CLI 29.6.1, Compose 5.3.0, WSL 2.7.10. PostgreSQL 17 Alpine, `127.0.0.1:5432`, UTC, healthcheck, `unless-stopped`, named volume. migrations `20260716_init`, `20260720_notification_delivery_outcome_policy` 적용·zero drift.
- **front Phase 1**: `design/` 5 PNG + prototype 기준 dark-first login/tutorial/home/ranking/my, Task CRUD prototype, loading/empty/error/offline, theme/logout. 909×540·390×844 Browser QA. 실제 OAuth/backend/FCM/WebSocket/DB 연결 미구현.
- **다음 작업**: front secure session·API client → 12C permission + logout/account-switch Installation rotation + authenticated current-state sync/display → test project/device ADC·FCM sandbox → dispatch 활성 판단 → WebSocket → Redis/batch.
- **운영**: `.ai/agents/README.md` 역할 계약; 제품 조사·구현·review는 적합한 역할 sub-agent, main은 계획·승인·memory·diff 통합. 고위험은 `change-gate`, release 전 `release-audit`; audit JSONL은 증거·상태 이력.
- **오류 재사용**: 오류 작업 전 `error-resolution-playbook.md` 검색. 환경·root cause 일치 `VERIFIED`만 현재 범위에서 적용·재검증. `MITIGATION_ONLY`는 gate·잔여 위험 유지.
- **memory backup**: `*.original.md`는 local 복구 snapshot, Git 제외·비활성. 2026-07-20 Anthropic compression script의 Windows CP949 bug로 기존 context pre-image가 손실돼 Git HEAD·plan·architecture·checklist에서 상세 snapshot을 재구성했다.
- **Obsidian Vault 연결(2026-07-20)**: `C:\AiWiki`를 Obsidian 1.12.7의 단일 local Vault로 등록했다. `C:\AiWiki\AiProject`는 여러 프로젝트를 담는 일반 directory다.
- **Obsidian DSM 문서 큐레이션·일반 컨테이너 전환(2026-07-22)**: `C:\AiWiki\AiProject\DSM`은 일반 directory이고, `Current` junction은 `C:\DEV\.ai\docs`, `Planning` junction은 `C:\DEV\Planing Document`를 가리킨다. `Overview.md`는 Current 4개와 Planning v1.3 4개를 연결하며 기획 문서 4개의 architecture 링크도 새 Current 경로를 사용한다. 원본 `C:\DEV`와 두 junction target은 보존됐다. 전환 전 IndexedDB cache는 삭제하지 않고 `.pre-dsm-20260721` exact backup으로 이동했으며 Obsidian workspace와 새 cache가 정상 생성됐다. 사용자 action-time 승인 후 stale 제외 필터 11개를 UI에서 제거하고 보관함 cache를 재구축했으며 `app.json` readback은 `userIgnoreFilters: null`이다. Quick Switcher에서 Overview·현재 architecture는 검색되고 `node_modules`·`AGENTS.md`·`superpowers`는 파일 결과가 없음을 확인했다. 복구 절차는 `ER-20260722-001`로 기록했다.
- **오프라인 학습 사이트 조사(2026-08-08)**: HTML 생성 전 root checkout inventory를 완료했다. 기본 제외 뒤 211 files이며 application text 후보는 124 files·13,168 lines(`DSM_Back` 88·8,787, `DSM_Front` 36·4,381)이다. repository support 55, generated lock 2, media/binary 29, sensitive `.env` 1로 분류했다. 상세 목록은 `.ai/docs/2026-08-08-offline-learning-site-project-analysis.md`다.
- **학습 원본 기준**: 현재 root `codex/m12b-front-prototype-checkpoint` HEAD `960f02b`를 사용자가 corpus 기준으로 선택했다. clean 별도 worktree `codex/front-secure-session-rest-client` HEAD `bb712ba`는 제외한다.
- **학습 사이트 선택 승인(2026-08-08)**: 사용자 `A ㄱ`으로 `C:\DEV` root `960f02b`와 pilot A 15 files를 선택했다. 실제 source 확인 결과 Task mutation은 NotificationsService를 직접 호출하지 않고 Serializable Prisma transaction 안에서 Task·schedule·delivery를 변경하며 ScoresService만 호출한다. pilot 명칭은 “Task 변경 → 점수 재계산 + 알림 예약 상태 동기화”로 정정했다.
- **학습 사이트 디자인 승인(2026-08-08)**: 사용자 `ㄱ`으로 project map, 정확한 Task architecture, desktop file study, mobile file study의 네 정정 concept을 승인했다. source code는 이미지 생성 서비스에 전송하지 않았다. 승인 asset은 `docs/superpowers/specs/assets/offline-learning-site/`, 서면 명세는 `docs/superpowers/specs/2026-08-08-offline-learning-site-design.md`에 고정했다.
- **학습 사이트 명세 검증**: 네 workspace asset은 생성 원본과 SHA-256이 각각 일치하고 Markdown image target 4개가 모두 존재한다. 명세 필수 section·사실 문구·placeholder scan·trailing whitespace·`git diff --check`를 통과했으며 `DSM_Back`·`DSM_Front` diff와 `learning-site/`·`tools/learning-site/` 생성은 없다.
- **학습 사이트 서면 명세 승인(2026-08-08)**: 사용자 `명세 승인`으로 디자인 명세 검토 gate를 통과했다. 상태 line을 승인 완료로 갱신했다.
- **학습 사이트 상세 구현 계획**: `docs/superpowers/plans/2026-08-08-offline-learning-site-pilot-a.md`에 Node >=22 built-in generator/verifier, 15-file Pilot output, source integrity, offline runtime, Browser QA와 memory closure를 27 tasks·task당 최대 2 files로 고정했다. Remaining 109 files는 Pilot 승인 뒤 별도 10~20-file batch 계획 대상이다.
- **학습 사이트 실행 선택(2026-08-08)**: 사용자 `1 ㄱ`의 Subagent-Driven 선택은 worktree/Git write와 tooling 역할 부재로 preflight에서 중단했고, 후속 사용자 `2`로 current root inline/no-Git 실행으로 전환했다. Git write는 승인되지 않았고 실행하지 않았다.
- **학습 사이트 Pilot A 구현**: `tools/learning-site/`에 Node built-in manifest/source/symbol/syntax/content/render/pages/style/runtime/generator/verifier와 테스트를 구현하고 `learning-site/`에 15 source pages, 6 overview pages, 3 local assets를 생성했다. application corpus는 124 files·13,168 lines, processed=15, remaining=109, missing=0이다.
- **학습 사이트 검증**: 전체 48 tests PASS. `verification-report.json`은 15 source text/SHA-256/bytes/lines/line ending, 510 local links/fragments, offline/search/progress와 prohibited edge를 PASS로 기록한다. `DSM_Back`·`DSM_Front` diff는 없고 `.env`는 조회하지 않았다.
- **학습 사이트 Browser QA**: 사용자 승인으로 `C:\DEV\learning-site`만 제공하는 임시 localhost 서버를 사용해 인앱 Chromium 1440×900·390×844 QA를 완료했다. 검색·theme/read persistence·파일 62:38·mobile sheets·diagram zoom/reset/keyboard/pointer drag·architecture 금지 노드가 PASS다. QA 중 hidden notice, desktop 읽음 제어, 검색 metadata, mobile sheet, 한국어 제목 줄바꿈, mobile home 여백을 TDD로 수정했다. 상세 결과는 `learning-site/qa-report.md`다.
- **학습 사이트 Pilot A 승인(2026-08-09)**: 사용자 `Pilot A 승인`으로 15 source pages·6 overview pages, 48 tests, full verifier와 1440×900·390×844 Browser QA 결과를 승인했다.
- **학습 사이트 현재 gate**: processed=15, remaining=109, missing=0이다. 다음 10~20-file 묶음은 B `Social Auth와 refresh rotation` 또는 C `Expo 제품 prototype` 중 범위를 선택하고 별도 design spec·상세 계획을 승인받기 전에는 생성하지 않는다.
- **학습 사이트 Batch B 선택(2026-08-09)**: 사용자 `B ㄱ`으로 `Social Auth와 refresh rotation`을 선택했다. 신규 후보는 `main.ts`, `app.bootstrap.ts`, Auth module/controller/service/guard, DTO 3, type 2, controller/service spec의 13개·883줄이다. Pilot A의 AppModule·Prisma schema/service는 관계 링크로 재사용한다.
- **Batch B 사실 경계**: Google은 configured audience로 ID token을 검증하고 Kakao는 `/v2/user/me`를 호출하며 Apple은 미구현 409다. refresh는 id/secret PK lookup + bcrypt + transaction conditional revoke/replace이고, logout invalid input은 no-op이다. `/auth/me`는 현재 `userId`만 반환하며 CORS는 `origin: true`, `credentials: true`다.
- **Batch B 승인(2026-08-09)**: 구현·정적 검증·1440×900 및 390×844 Browser QA 결과를 사용자가 `Batch B 승인`으로 승인했다. Batch B gate는 완료됐으며 다음 batch는 별도 범위 선택·명세·계획 승인 전 시작하지 않는다.
- **Batch B 설명 선택(2026-08-09)**: 사용자 `1`로 균형형을 선택했다. Social login·JWT guard·refresh rotation·logout을 고르게 다루고 AuthService와 service spec을 가장 깊게 설명한다.
- **Batch B 설계 1절 승인(2026-08-09)**: 사용자 `ㄱ`으로 Pilot A를 독립 재현하면서 Batch B는 A∪B의 processed 상태를 계산하는 stage-aware 누적 batch registry와 social-login, refresh-rotation, jwt-session, auth-session diagram, auth-session exercises의 신규 overview 5개 정보 구조를 승인했다. 구현 승인은 아니다.
- **Batch B 설계 2절 승인(2026-08-09)**: 사용자 `ㄱ`으로 social login, JWT guard, refresh single-winner rotation, logout no-op의 실제 source/test 흐름과 비유·정확 용어·원본 코드 분리, confirmed/conditional/`확인 필요` 상태, 위험·한계 표시를 승인했다. raw token/secret 비노출과 Serializable 오표기 금지를 포함하며 구현 승인은 아니다.
- **Batch B 설계 3절 승인(2026-08-09)**: 사용자 `ㄱ`으로 manifest stage registry, 별도 `content/batch-b.mjs`, stage model 기반 generator/verifier/page 확장, TDD·28-source fidelity·예상 39 HTML·Pilot A 회귀·Browser QA·server 종료 기준을 승인했다. 구현 승인은 아니다.
- **Batch B written spec 승인(2026-08-09)**: 사용자 `ㄱ`으로 `docs/superpowers/specs/2026-08-09-offline-learning-site-batch-b-design.md`를 승인했다. controller spec과 rollback test의 실제 증거 범위, raw token 비노출, Serializable 오표기 금지를 포함한다.
- **Batch B 상세 구현 계획 승인·실행(2026-08-09)**: 사용자 `ㄱ`으로 `docs/superpowers/plans/2026-08-09-offline-learning-site-batch-b.md`의 14 tasks와 current root inline/no-Git 방식을 승인했다. `superpowers:executing-plans`·TDD로 Task 1부터 실행하며 task별 최대 2 files, 기존 CSS/JS 재사용, QA defect 시 새 exact allowlist gate를 유지한다. Git write와 제품 source 변경은 승인되지 않았다.
- **Batch B 구현·검증 완료(2026-08-09)**: stage-aware Pilot A/Batch B 생성, Auth source 13개와 overview 5개를 포함한 누적 HTML 39개·source page 28개·asset 3개를 생성했다. verifier는 links 903, processed 28, remaining 96, missing 0, excluded 9와 source fidelity/offline/search를 PASS로 기록했고 전체 Node test 66개가 통과했다.
- **Batch B Browser QA(2026-08-09)**: 인앱 Chromium 1440×900·390×844에서 home·검색·Auth source·social/refresh/JWT·diagram·exercise·mobile sheet를 검증했다. exercise path와 SHA-256의 document overflow를 TDD로 수정해 최종 폭은 각각 1425/1440, 375/390이다. console error/warn과 overlay는 없고 임시 server·port는 종료했다.
- **Batch B 범위 보존**: `DSM_Back/**`·`DSM_Front/**` diff는 비어 있고 `.env`는 읽거나 산출하지 않았다. dependency/network/DB·Docker·Firebase·제품 service와 Git write는 실행하지 않았다. 새 exposure·responsive 해결은 `ER-20260809-001`~`003`으로 기록했다.
- **Git branch publish·동기화(2026-08-25)**: root `codex/m12b-front-prototype-checkpoint`에 offline learning site commit `43145b6`과 handoff·AI workflow commit `c79a042`를 생성해 origin에 push했다. 별도 clean worktree `C:\DEV\fsr`의 ahead-1 commit `2a4e991`도 `origin/codex/front-secure-session-rest-client`에 push했다. fetch 뒤 root·fsr·main은 모두 ahead/behind `0/0`이어서 pull은 불필요했다. learning tests 66, full verifier 28 sources, backend unit 198·e2e 2, Nest build와 front TypeScript가 통과했다. `fsr/`은 root commit에서 제외했고 merge·PR·force-push·worktree 삭제는 수행하지 않았다.
