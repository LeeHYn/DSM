# 목표
현재 디렉토리의 모든 파일을 지정된 GitHub 저장소(`https://github.com/LeeHYn/DSM.git`)에 빠짐없이 업로드합니다.

# 마일스톤
1. Git 로컬 저장소 상태 확인 및 초기화 (필요시)
2. `.gitignore` 설정 확인 (민감한 정보나 불필요한 파일이 올라가지 않도록 방지)
3. 원격 저장소(`origin`) 등록 및 URL 설정
4. 전체 파일 스테이징(`git add`) 및 커밋(`git commit`)
5. 원격 저장소로 푸시(`git push`)
