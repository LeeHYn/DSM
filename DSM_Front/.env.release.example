# Copy to .env.release.local; replace both values before release validation.
# This public client identifier is embedded in the APK; never add secrets here.
API_BASE_URL=https://api.example.invalid
GOOGLE_WEB_CLIENT_ID=
